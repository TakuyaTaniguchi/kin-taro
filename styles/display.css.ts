import { style , styleVariants} from '@vanilla-extract/css';

export const display = style({
    textAlign: "left",
    width: "100%",
    fontSize: "18px",
})


export const display_time = style({
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
})